<div class="col-md-3">
    <button data-saved="{{saved}}" data-category="{{category}}" data-flightAirline="{{Airline}}"
        data-flightPrice="{{Price}}" data-flightDuration="{{Duration}}" data-flightDepartureTime="{{DepartureTime}}"
        data-flightArrivalTime="{{ArrivalTime}}" class="btn btn-primary change-saved">
        {{#if saved}}RETURN TO LIST >>{{else}} Save {{/if}} </button>
</div>